```Python
# 列表用[]表示
shop_list=["平板"]
print(shop_list)

# 使用append往列表中添加元素
shop_list.append("鼠标")
print(shop_list)

# 使用remove删除列表已有元素,不能删除没有的元素,否则会报错
shop_list.remove("平板")
print(shop_list)

# 列表中的元素种类可以不同
shop_list.append(233)
shop_list.append(66.6)
shop_list.append(None)
shop_list.append(True)
print(shop_list)

# len求列表的元素个数
print(len(shop_list))

# 通过索引获取元素
print(shop_list[0])

# 通过索引修改
shop_list[0]="test"
print(shop_list[0])

```
 **输出**
```Python
['平板']
['平板', '鼠标']
['鼠标']
['鼠标', 233, 66.6, None, True]
5
鼠标
['test', 233, 66.6, None, True]
```

列表是可变的,而其他数据类型不可变
以字符串为例:
```Python
str="hello"
print(str.upper())
print(str)
# 调用upper()后,str仍是"hello"

str=str.upper()
print(str)

# shop_list=shop_list.append("键盘")
# 列表不应这样
```
**输出**
```
HELLO
hello
HELLO
```
# 列表相关的函数
```Python
num=[1,4,-3,0,23,-55]
print(max(num))
print(min(num))
print(sorted(num))
# 均不改变原列表

print(num)
```
**输出**
```Python
23
-55
[-55, -3, 0, 1, 4, 23]
[1, 4, -3, 0, 23, -55]
```